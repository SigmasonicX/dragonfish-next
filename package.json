{
	"name": "dragonfish",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev --parallel",
		"dev:client": "yarn workspace @dragonfish/client run dev",
		"start": "turbo run start",
		"start:api": "turbo run start --scope='@dragonfish/api'"
	},
	"turbo": {
		"baseBranch": "origin/main",
		"pipeline": {
			"build": {
				"dependsOn": [
					"^build"
				],
				"outputs": [
					".svelte-kit/**",
					"dist"
				]
			},
			"test": {
				"dependsOn": [
					"^build"
				],
				"outputs": []
			},
			"lint": {
				"outputs": []
			},
			"dev": {
				"cache": false
			},
			"start": {
				"dependsOn": [
					"^build"
				]
			}
		}
	},
	"devDependencies": {
		"prettier": "^2.5.1",
		"prettier-plugin-svelte": "^2.4.0",
		"turbo": "^1.0.13",
		"typescript": "^4.5.4"
	}
}
